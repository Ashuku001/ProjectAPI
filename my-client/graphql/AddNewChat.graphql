mutation addChat($chat: ChatInput!) {
  addChat(chat: $chat) {
    id
    customer {
      id
      whatsapp_name
      phone_number
      first_name
      last_name
      __typename
    }
    lastMessage {
      id
      createdAt
      text
      __typename
    }
    __typename
  }
}
