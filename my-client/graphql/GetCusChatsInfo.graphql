query GetCusInChatInfo($chatId: Int!){
    chat(chatId: $chatId){
        customer {
            id
            first_name
            last_name
            phone_number
            __typename
       }
    }
}